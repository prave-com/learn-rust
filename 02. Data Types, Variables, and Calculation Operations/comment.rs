fn main() {
    // This is a single-line comment
    // println!("{}", 5);
    // println!("{}", 6);

    /*
        This is a multi-line comment.
        It can span across multiple lines.
        Rust will ignore everything inside this block.
        println!("{}", 10);
        println!("{}", 15);
    */

    /* /* This is nested comment */ */
}
